# Canvas Instructions

- 画布持久状态、临时交互状态和渲染派生状态必须分开；拖拽预览、选择框和悬浮状态不得写入持久快照。
- 节点和连线修改应经过现有命令、更新与撤销重做边界，不要在视图组件中直接散落状态写入。
- 新节点类型必须补齐创建、渲染、复制、保存、恢复和兼容降级路径。
- Alt 拖拽、批量选择、分组、缩放与连线等交互修改需要覆盖指针生命周期和取消路径。
- 修改画布 schema 时必须兼容旧快照并增加回归测试；不得仅依靠手工打开新项目验证。
- 优先从 `canvas-client.tsx` 提取稳定边界，避免同时进行无关的大规模重排。
