# AGENTS.md

## 对话语言

- 始终使用中文（简体）进行面向用户的对话、总结、提问和计划说明。
- 代码、标识符、文件名、环境变量、commit message 等遵循各自技术惯例，不强制中文化。

## 项目背景

- 这是一个基于 Next.js + Supabase 的项目。
- 后续开发需要优先遵循当前项目已有的技术栈、目录结构和代码风格。
- 在新增功能、重构或修复问题前，应先阅读相关现有代码，尽量沿用项目已有组件、工具函数、样式约定和数据访问方式。

## 项目文档目录约定

- 相关项目文档统一放在 `/docs/` 目录下。
- `/docs/prd.md`：存放项目需求文档、功能规格、已确认的产品决策。
- `/docs/images/`：存放截图、界面参考图、流程图等图片资料。
- `/docs/api/`：存放 API 文档、接口约定、第三方服务对接说明。

## 需求沉淀规则

- 当用户讨论功能实现、产品需求、业务规则或交互细节时，如果某些需求已经明确，应及时整理并写入 `/docs/prd.md`。
- 写入 PRD 前，需要基于已经确认的信息，不要把未经确认的猜测当成最终需求。
- 如果需求还不完整，可以先记录为“待确认问题”或“开放问题”。
- PRD 更新应尽量保持结构清晰，区分“已确认需求”“产品决策”“待确认问题”和“开放问题”。

## Supabase 操作规则

- 执行和 Supabase 相关的任务时，尽可能优先使用 Supabase MCP 完成操作，包括但不限于：
  - 数据库表结构查看
  - SQL 查询
  - RLS 策略检查和创建
  - Storage Bucket 的查看和创建
- 涉及数据库结构、权限策略或存储桶变更时，应说明操作目的，并在必要时记录到 `/docs/prd.md` 或 `/docs/api/`。

## 安全和配置约定

- 不要把 Supabase service role key、数据库密码、访问令牌等敏感信息写入前端代码或提交到仓库。
- 前端只能使用 `NEXT_PUBLIC_` 开头的公开环境变量。
- 涉及用户数据访问时，需要优先考虑 RLS 策略和最小权限原则。
- 不要在日志、文档、示例代码或错误信息中泄露敏感配置。

## 编辑方式

- 如果 `AGENTS.md` 已存在，应在保留原有有效内容的基础上合并更新，不要无意义覆盖。
- 如果 `/docs/`、`/docs/prd.md`、`/docs/images/`、`/docs/api/` 不存在，可以一并创建。
- 编辑文件时应尽量保持改动聚焦，避免顺手重构或改动与当前任务无关的内容。
