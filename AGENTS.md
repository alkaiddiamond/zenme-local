# Repository Instructions

## 对话语言

- 始终使用中文（简体）进行面向用户的对话、总结、提问和计划说明。
- 代码、标识符、文件名、环境变量和 commit message 遵循技术惯例。

## 开始工作前

- 先运行 `git status --short`，保留用户已有改动，不覆盖或顺手格式化无关文件。
- 修改前阅读受影响的生产路径、最近测试和同类实现；优先复用已有组件、工具函数和数据访问方式。
- 修复缺陷时优先增加能够证明问题的回归测试。无法复现时明确说明限制，不猜测。
- 只实现与请求、失败测试或已确认兼容约束直接相关的最小改动。
- 不创建或切换分支，不提交、推送、发布或更改仓库设置，除非用户明确要求。

## 项目边界

- `app/`：Next.js 页面和只允许本机访问的 API。
- `components/zenme/`：产品界面、画布、节点和阅读工作区。
- `components/ui/`：通用 UI 原语。
- `desktop/`：Electron 主进程、IPC、窗口生命周期和打包工具；修改前阅读 `desktop/AGENTS.md`。
- `lib/local/`：本地数据、迁移、备份和原子写入；修改前阅读 `lib/local/AGENTS.md`。
- `components/zenme/canvas/`：画布状态和交互；修改前阅读该目录的 `AGENTS.md`。
- `docs/`：与代码版本绑定的架构、安全、数据和发布文档。

Zenme 是 Next.js + Electron 的纯本地桌面应用，不提供账号、远程数据库或自动云同步。本地 HTTP 服务只允许 loopback 与同源访问，桌面窗口是正式产品入口。

## 文档边界

- 产品需求、产品概念、设计资料、路线图和研究文档维护在独立 `zenme-doc` 仓库。本地默认使用相邻目录 `../zenme-doc`，不得写死某台机器的绝对路径。
- 架构、数据格式、迁移、安全、打包发布和故障排查维护在本仓库 `docs/`，并随相关代码一起更新。
- 已确认需求及时写入 `zenme-doc/prd.md`；未经确认的内容必须标记为待确认或开放问题。

## 实现规则

- 通用能力优先采用维护活跃、许可证兼容、桌面端可用的成熟开源方案；引入前核对许可证、体积、安全记录和替换成本。
- API Key 和令牌只能保存在本地数据目录，不得写入前端、日志、文档、示例或仓库。
- 用户输入路径必须经过路径段校验并限定在允许根目录内。
- 本地 API 和新增管理接口必须复用 loopback、同源和统一错误边界。
- 持久化结构变化必须提供向前迁移和旧数据回归测试；测试不得使用真实用户数据目录。
- 用户可见或跨进程桌面行为需要自动化证明，不能覆盖时必须记录真机验证结果。

## 验证与交付

1. 迭代时运行最窄的相关测试。
2. 一般代码改动至少运行 `npm run check`。
3. 声明可发布或可合并前运行 `npm run verify`；桌面发布还需对应平台打包和 `npm run desktop:smoke`。
4. 交付前检查 `git diff --check`、`git diff` 和 `git status --short`。
5. 明确区分通过、失败、跳过、阻塞和未运行；Windows 构建不能替代 macOS Intel 真机验证。

## 编辑方式

- 保持改动聚焦，避免无意义覆盖现有文件。
- 不提交生成目录、缓存、日志、真实数据或构建产物。
- 工程文档索引见 `docs/README.md`，贡献流程见 `CONTRIBUTING.md`。
